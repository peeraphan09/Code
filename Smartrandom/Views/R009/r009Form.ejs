<%- include("../_header"); -%>
<body class="body">
    <div class="container mt-5">
        <h3 class="text-center" style="color: black;">Add</h3>
        <div class="row justify-content-center mt-4">
            <div class="col-md-6">
                <form action="/r009/add" method="post">
                    <div class="form-group">
                        <label for="rg09">Name</label>
                        <input type="text" name="rg09" placeholder="Name" class="form-control">
                    </div> 
                    <!-- <div class="form-group">
                        <label for="install">Edit Installation Date</label>
                        <input type="text" name="install" id="install" placeholder="YYYY-MM-DD"  class="form-control">
                    </div>
                    
                    <script>
                        $(function() {
                            // เลือก input field ที่มี id เป็น 'install' และใช้ jQuery UI Datepicker
                            $('#install').datepicker({
                                dateFormat: 'yy-mm-dd' // รูปแบบวันที่ที่ต้องการ (YYYY-MM-DD)
                            });
                        });
                    </script> -->
                    <div class="form-group">
                        <label for="rh09">Vsion</label>
                        <select class="form-control" name="rh09"> <!-- แก้ชื่อตรงนี้ -->
                            <option value="">Choose Name</option>
                            <% if (s009) { %>
                                <% for (var i = 0; i < s009.length; i++) { %>
                                    <option value="<%= s009[i].si09 %>"><%= s009[i].sk09  %></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </form>
                <a href="/s009" class="btn btn-secondary">Back</a>
            </div>
        </div>
    </div>
</body>
<%- include("../_footer"); -%>