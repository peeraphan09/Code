<%- include("../_header"); -%>
<body class="body">
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <br>
                <h3 class="text-center">Edit</h3>
                <form action="/r009/update/<%= data1&& data1[0] ? data1[0].rf09 : '' %>" method="post" class="w-50">
                    <div class="form-group">
                        <label for="rg09"> RG </label>
                        <input type="text" id="rg09" name="rg09" placeholder="rg09" value="<%= data1 && data1[0] ? data1[0].rg09 : '' %>" class="form-control">
                    </div>
                    <br>
                    <div class="form-group">
                        <label for="rh09">Rh</label>
                        <select class="form-control" name="rh09">
                            <option value="">กรุณาเลือก</option>
                            <% if (data2) { %>
                                <% for (var i = 0; i < data2.length; i++) { %>
                                    <option value="<%= data2[i].si09 %>" <% if(data1 && data1[0] && data2[i].si09==data1[0].rh09){ %>selected<% } %> ><%= data2[i].sk09  %></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <br>
                    <button type="submit" class="btn btn-primary">Save</button>
                    <a href="/r009" class="btn btn-danger">Back</a>
                </form>
            </div>
        </div>
    </div>
</body>
<%- include("../_footer"); -%>