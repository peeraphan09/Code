<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>User Panel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { min-height: 100vh; margin: 0; display: flex; flex-direction: row; }
    .sidebar { width: 200px; background-color: #343a40; color: white; min-height: 100vh; }
    .sidebar a { display: block; color: white; padding: 10px 15px; text-decoration: none; }
    .sidebar a:hover { background-color: #495057; }
    .main-wrapper { flex: 1; display: flex; flex-direction: column; }
    .topbar { background-color: #153f69; padding: 10px 20px; display: flex; justify-content: flex-end; width: 100%; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
    .topbar a { margin-left: 15px; text-decoration: none; color: #fff; }
    .content { flex: 1; padding: 20px; }
  </style>
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar">
    <h4 class="text-center py-3">User Panel</h4>
    <a href="/homeUS">หน้าแรก</a>
    <a href="/user/contracts">สัญญาของฉัน</a>
    <a href="/user/bills">บิลน้ำ/ไฟ</a>
    <a href="/user/reports">แจ้งปัญหา/คำร้อง</a>
    <a href="/logout">ออกจากระบบ</a>
  </div>

  <!-- Main wrapper -->
  <div class="main-wrapper">
    <!-- Topbar -->
    <div class="topbar">
      <a href="/logout">ออกจากระบบ</a>
    </div>

    <!-- Content -->
    <div class="content">
