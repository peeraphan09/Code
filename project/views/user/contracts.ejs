<%- include('../_header1') %>
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>สัญญาของฉัน</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h3>📑 สัญญาของฉัน</h3>
    <p class="text-muted">แสดงสัญญาเช่าที่แอดมินบันทึกให้คุณ</p>

    <% if (contracts.length === 0) { %>
      <div class="alert alert-warning">คุณยังไม่มีสัญญาเช่า</div>
    <% } else { %>
      <table class="table table-bordered text-center">
        <thead class="table-light">
          <tr>
            <th>ห้อง</th>
            <th>ค่าเช่า (บาท)</th>
            <th>สถานะ</th>
            <th>วันที่สร้าง</th>
            <th>รูปภาพสัญญา</th>
          </tr>
        </thead>
        <tbody>
          <% contracts.forEach(c => { %>
            <tr>
              <td><%= c.room_number %></td>
              <td><%= c.price ? c.price.toFixed(2) : '-' %></td>
              <td>
                <% if (c.status === "เสร็จสิ้น") { %>
                  <span class="badge bg-success"><%= c.status %></span>
                <% } else { %>
                  <span class="badge bg-warning"><%= c.status %></span>
                <% } %>
              </td>
              <td><%= c.created_at ? new Date(c.created_at).toISOString().slice(0,10) : '-' %></td>
              <td>
                <% if (c.contract_image) { %>
                  <a href="/uploads/contracts/<%= c.contract_image %>" target="_blank" class="btn btn-sm btn-info">
                    <i class="fas fa-file-image"></i> ดูรูป
                  </a>
                <% } else { %>
                  <span class="text-muted">ไม่มีไฟล์</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/homeUS" class="btn btn-secondary mt-3">⬅ กลับหน้าหลัก</a>
  </div>
</body>
</html>
<%- include('../_footer1') %>