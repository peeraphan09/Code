<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Header for User</title>
    <link rel="stylesheet" href="/styles.css"> <!-- หากมีการใช้ไฟล์ CSS -->
    <link rel="stylesheet" href="/css/profile.css">
</head>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    body {
        font-family: Arial, sans-serif;
    }

    /* แถบบาร์ด้านบน */
    .navbar {
        background-color: #007bff;
        color: white;
        padding: 15px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .nav-left {
        display: flex;
        align-items: center;
    }

    .nav-right {
        display: flex;
        gap: 10px;
    }

    .menu-btn {
        font-size: 24px;
        cursor: pointer;
        background: none;
        border: none;
        color: white;
        margin-right: 15px;
    }

    .nav-links {
        display: flex;
        gap: 20px;
    }

    .nav-links a,
    .auth-btn {
        color: white;
        text-decoration: none;
        font-size: 18px;
    }

    .nav-links a:hover,
    .auth-btn:hover {
        text-decoration: underline;
    }

    /* Sidebar */
    .sidebar {
        width: 250px;
        position: fixed;
        left: -250px;
        top: 0;
        height: 100%;
        background-color: #333;
        color: white;
        padding-top: 60px;
        transition: 0.3s;
    }

    .sidebar a {
        display: block;
        padding: 15px;
        color: white;
        text-decoration: none;
        font-size: 18px;
        border-bottom: 1px solid gray;
    }

    .sidebar a:hover {
        background-color: #575757;
    }

    .close-btn {
        position: absolute;
        top: 10px;
        right: 20px;
        font-size: 24px;
        cursor: pointer;
    }

    /* Overlay */
    .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.5);
        display: none;
    }

    .content {
        padding: 20px;
        text-align: center;
        margin-top: 50px;
    }

    /* ปุ่มเข้าสู่ระบบและสมัครสมาชิก */
    .auth-btn {
        padding: 10px 15px;
        background-color: #0056b3;
        border-radius: 5px;
    }

    .auth-btn:hover {
        background-color: #004494;
    }
    .profile-link {
    display: flex;
    align-items: center;
    gap: 8px;
    color: white;
    text-decoration: none;
}

.profile-img {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid white;
}

</style>
<!-- SweetAlert2 CDN -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

</head>
<body>

<!-- Navbar -->
<div class="navbar">
    <div class="nav-left">
        <button class="menu-btn" onclick="openSidebar()">☰</button>
        <div class="nav-links">
            <a href="/homeUS">หน้าหลัก</a>
            <a href="/contact">ติดต่อเรา</a>
            <a href="/about">เกี่ยวกับ</a>
        </div>
    </div>
    <div class="nav-right">
        <% if (!user) { %>
            <a href="/login" class="auth-btn">เข้าสู่ระบบ</a>
            <a href="/register" class="auth-btn">สมัครสมาชิก</a>
        <% } else { %>
            <a href="/user/profile" class="profile-link">
                <img src="<%= user.profile_image || '/images/default-profile.png' %>" alt="Profile" class="profile-img">
                <span><%= user.first_name %> <%= user.last_name %></span>
            </a>
        <% } %>
    </div>
    
    <div id="overlay" class="overlay" onclick="closeSidebar()"></div>

    <!-- Sidebar -->
    <div id="sidebar" class="sidebar">
        <span class="close-btn" onclick="closeSidebar()">×</span>
        <a href="/users/equipmentList">รายการอุปกรณ์กีฬา</a> <!-- เพิ่มเส้นทางไปที่หน้า equipmentList -->
        <a href="/user/orderList">สถานะ/คำสั่ง</a> <!-- เพิ่มเส้นทางไปที่หน้า equipmentApproval -->
        <a href="/user/history">ดูประวัติการยืมคืน</a>
        <a href="/user/profile">ตั้งค่าโปรไฟล์</a>
        <a href="/logout" class="auth-btn" style="background-color: red;">ออกจากระบบ</a>
    </div>
</div>
</header>


<script>
    function openSidebar() {
        document.getElementById("sidebar").style.left = "0";
        document.getElementById("overlay").style.display = "block";
    }

    function closeSidebar() {
        document.getElementById("sidebar").style.left = "-250px";
        document.getElementById("overlay").style.display = "none";
    }
</script>
<!-- เนื้อหาของหน้า -->


</body>
</html>
