<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>อนุมัติการยืมอุปกรณ์</title>
</head>
<body>
    <h1>รายการขอยืมอุปกรณ์</h1>
<table>
    <thead>
        <tr>
            <th>ชื่อผู้ใช้</th>
            <th>ชื่ออุปกรณ์</th>
            <th>จำนวน</th>
            <th>อนุมัติ</th>
            <th>ปฏิเสธ</th>
        </tr>
    </thead>
    <tbody>
        <% borrowRequests.forEach(function(request) { %>
            <tr>
                <td><%= request.username %></td>
                <td><%= request.equipment_name %></td>
                <td><%= request.quantity %></td>
                <td><a href="/admin/approve/<%= request.id %>">✅</a></td>
                <td><a href="/admin/reject/<%= request.id %>">❌</a></td>
            </tr>
        <% }); %>
    </tbody>
</table>
</body>
</html>
