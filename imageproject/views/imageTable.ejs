<!-- 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Table</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <h1>Gallery</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/list">Image List</a></li>
                <li><a href="/images">Image Upload</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h1>Image Upload</h1>
        <table class="image-table">
            <thead>
                <tr>
                    <th>Filename</th>
                    <th>Timestamp</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% images.forEach(function(image) { %>
                    <tr id="image-<%= image.filename %>">
                        <td><%= image.filename %></td>
                        <td><%= image.timestamp %></td>
                        <td>
                            <img src="/images/<%= image.filename %>" alt="<%= image.filename %>" class="thumbnail" onclick="openModal('<%= image.filename %>')">
                        </td>
                        <td>
                            <button class="delete-button" onclick="deleteImage('<%= image.filename %>')"><i class="fas fa-trash icon"></i>Delete</button>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        
        <div id="imageModal" class="modal">
            <span class="close" onclick="closeModal()">&times;</span>
            <img class="modal-content" id="modalImage">
        </div>
    </main>
    <script>
        function openModal(filename) {
            var modal = document.getElementById("imageModal");
            var modalImage = document.getElementById("modalImage");
            modalImage.src = "/images/" + filename;
            modal.style.display = "block";
        }

        function closeModal() {
            var modal = document.getElementById("imageModal");
            modal.style.display = "none";
        }

        function deleteImage(filename) {
            if (confirm('Are you sure you want to delete this image?')) {
                fetch('/images/delete', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ filename: filename })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.error) {
                        alert('Error: ' + data.error);
                    } else {
                        alert(data.message);
                        const row = document.getElementById('image-' + filename);
                        if (row) {
                            row.remove();
                        }
                    }
                });
            }
        }
    </script>
</body>
</html>
 -->



 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Table</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Modal styles */
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.4);
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            margin: auto;
            display: block;
            width: 80%;
            max-width: 700px;
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

        .thumbnail {
            width: 100px;
            height: auto;
        }

        .image-table {
            width: 100%;
            border-collapse: collapse;
        }

        .image-table th, .image-table td {
            border: 1px solid #ddd;
            padding: 8px;
        }

        .image-table th {
            background-color: #f4f4f4;
            text-align: left;
        }

        .delete-button {
            background-color: #f44336;
            border: none;
            color: white;
            padding: 8px 16px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
        }

        .icon {
            margin-right: 4px;
        }

        /* Filter styles */
        .filter-container {
            margin-bottom: 15px;
        }

        #dateFilter {
            padding: 5px;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <header>
        <h1>Gallery</h1>
        <nav>
            <ul>
                <li><a href="/home">Home</a></li>
                <li><a href="/list">Image List</a></li>
                <li><a href="/images">Image Upload</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <h1>Image Upload</h1>

        <!-- Date Filter -->
        <div class="filter-container">
            <label for="dateFilter">Filter by Date:</label>
            <select id="dateFilter" onchange="filterByDate()">
                <option value="">Select a Date</option>
                <% 
                    let dates = [];
                    images.forEach(function(image) {
                        let date = new Date(image.timestamp).toLocaleDateString('en-US', { timeZone: 'Asia/Bangkok' });
                        if (!dates.includes(date)) {
                            dates.push(date);
                        }
                    });
                    dates.forEach(function(date) { 
                %>
                    <option value="<%= date %>"><%= date %></option>
                <% }); %>
            </select>
        </div>

        <!-- Image Table -->
        <table class="image-table">
            <thead>
                <tr>
                    <th>Filename</th>
                    <th>Timestamp</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="imageTableBody">
                <% images.forEach(function(image) { 
                    let date = new Date(image.timestamp).toLocaleDateString('en-US', { timeZone: 'Asia/Bangkok' });
                %>
                    <tr id="image-<%= image.filename %>" data-date="<%= date %>">
                        <td><%= image.filename %></td>
                        <td><%= image.timestamp %></td>
                        <td>
                            <img src="/images/<%= image.filename %>" alt="<%= image.filename %>" class="thumbnail" onclick="openModal('<%= image.filename %>')">
                        </td>
                        <td>
                            <button class="delete-button" onclick="deleteImage('<%= image.filename %>')">
                                <i class="fas fa-trash icon"></i> Delete
                            </button>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <!-- Modal for Viewing Images -->
        <div id="imageModal" class="modal">
            <span class="close" onclick="closeModal()">&times;</span>
            <img class="modal-content" id="modalImage">
        </div>
    </main>

    <script>
        function openModal(filename) {
            var modal = document.getElementById("imageModal");
            var modalImage = document.getElementById("modalImage");
            modalImage.src = "/images/" + filename;
            modal.style.display = "flex";
        }

        function closeModal() {
            var modal = document.getElementById("imageModal");
            modal.style.display = "none";
        }

        function deleteImage(filename) {
            if (confirm('Are you sure you want to delete this image?')) {
                fetch('/images/delete', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ filename: filename })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.error) {
                        alert('Error: ' + data.error);
                    } else {
                        alert(data.message);
                        const row = document.getElementById('image-' + filename);
                        if (row) {
                            row.remove();
                        }
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('An error occurred while deleting the image.');
                });
            }
        }

        function filterByDate() {
            let selectedDate = document.getElementById("dateFilter").value;
            let rows = document.querySelectorAll("#imageTableBody tr");

            rows.forEach(row => {
                let rowDate = row.getAttribute("data-date");
                if (selectedDate === "" || rowDate === selectedDate) {
                    row.style.display = "";
                } else {
                    row.style.display = "none";
                }
            });
        }
    </script>
</body>
</html>
 



