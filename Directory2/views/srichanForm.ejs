<%- include("_header"); -%>
<br>
<br>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-5">
            <form action="/srichan/add" method="post">
                <div class="form-group">
                    <label for="name34">Name</label>
                    <input type="text" name="name34" placeholder="Name" class="form-control">
                </div>
                <div class="form-group">
                    <label for="a34">student09</label>
                    <select class="form-control" name="a34">
                        <option value="">กรุณาเลือก</option>
                        <% if (data2) { %>
                            <% for (var i = 0; i < data2.length; i++) { %>
                                <option value="<%= data2[i].id %>"><%= data2[i].student09 %></option>
                            <% } %>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label for="b34">student28</label>
                    <select class="form-control" name="b34">
                        <option value="">กรุณาเลือก</option>
                        <% if (data1) { %>
                            <% for (var i = 0; i < data1.length; i++) { %>
                                <option value="<%= data1[i].id %>"><%= data1[i].student28 %></option>
                            <% } %>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label for="Address34">Address</label>
                    <input type="text" name="Address34" placeholder="Address" class="form-control">
                </div>
                <br>
                <a href="/srichan/list" class="btn btn-danger">ย้อนกลับ</a>
                <button type="submit" class="btn btn-primary">Add</button>
            </form>
        </div>
    </div>
</div>
<%- include("_footer"); -%>
