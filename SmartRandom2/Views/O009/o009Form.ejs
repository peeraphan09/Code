<%- include("../_header"); -%>
<body class="body">
    <div class="container mt-5">
        <h3 class="text-center" style="color: black;">Add</h3>
        <div class="row justify-content-center mt-4">
            <div class="col-md-6">
                <form action="/o009/add" method="post">
                    <div class="form-group">
                        <label for="xy09">VERSION</label>
                        <input type="text" name="xy09" placeholder="VERSION" class="form-control">
                    </div> 
                    <div class="form-group">
                        <label for="of09">NAME</label>
                        <input type="text" name="of09" placeholder="NAME" class="form-control">
                    </div> 
                    <div class="form-group">
                        <label for="og09">Date</label>
                        <select class="form-control" name="og09"> <!-- แก้ชื่อตรงนี้ -->
                            <option value="">Choose Date</option>
                            <% if (q009) { %>
                                <% for (var i = 0; i < q009.length; i++) { %>
                                    <option value="<%= q009[i].qk09 %>"><%= q009[i].qm09.toISOString().split('T')[0]  %></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <!-- <div class="form-group">
                        <label for="install">Edit Installation Date</label>
                        <input type="text" name="install" id="install" placeholder="YYYY-MM-DD"  class="form-control">
                    </div>
                    
                    <script>
                        $(function() {
                            // เลือก input field ที่มี id เป็น 'install' และใช้ jQuery UI Datepicker
                            $('#install').datepicker({
                                dateFormat: 'yy-mm-dd' // รูปแบบวันที่ที่ต้องการ (YYYY-MM-DD)
                            });
                        });
                    </script> -->
<!--                     
                    <div class="form-group">
                        <label for="sk09">Version</label>
                        <input type="text" name="sk09" placeholder="Sk" class="form-control">
                    </div>  -->
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </form>
                <a href="/o009" class="btn btn-secondary">Back</a>
            </div>
        </div>
    </div>
</body>
<%- include("../_footer"); -%>