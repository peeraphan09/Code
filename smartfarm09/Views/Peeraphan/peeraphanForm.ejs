<%- include("../_header"); -%>
<body class="body">
    <div class="container mt-5">
        <h3 class="text-center" style="color: black;">Add</h3>
        <div class="row justify-content-center mt-4">
            <div class="col-md-6">
                <form action="/peeraphan/add" method="post">
                    <div class="form-group">
                        <label for="boonpun">Name</label>
                        <select class="form-control" name="boonpun"> <!-- แก้ชื่อตรงนี้ -->
                            <option value="">Choose Name</option>
                            <% if (boonpun09) { %>
                                <% for (var i = 0; i < boonpun09.length; i++) { %>
                                    <option value="<%= boonpun09[i].id %>"><%= boonpun09[i].name09 %></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <!-- <div class="form-group">
                        <label for="install">Edit Installation Date</label>
                        <input type="text" name="install" id="install" placeholder="YYYY-MM-DD"  class="form-control">
                    </div>
                    
                    <script>
                        $(function() {
                            // เลือก input field ที่มี id เป็น 'install' และใช้ jQuery UI Datepicker
                            $('#install').datepicker({
                                dateFormat: 'yy-mm-dd' // รูปแบบวันที่ที่ต้องการ (YYYY-MM-DD)
                            });
                        });
                    </script> -->
                    
                    <div class="form-group">
                        <label for="mirot09">Mirot</label>
                        <input type="text" name="mirot09" placeholder="Mirot" class="form-control">
                    </div> 
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </form>
                <a href="/peeraphan" class="btn btn-secondary">Back</a>
            </div>
        </div>
    </div>
</body>
<%- include("../_footer"); -%>